using System;

namespace LAB_2
{
    class Program
    {
        static void Main(string[] args)
        {
            /*LinkedList<string> linkedList = new LinkedList<string>(); //создание нового списка для данных типа string

            linkedList.Add("mathematics"); //добавление первого элемента 
            linkedList.Add("physics"); //добавление второго элемента 
            linkedList.Add("computer science"); //добавление третьего элемента 
            linkedList.Add("chemistry"); //добавление четвёртого элемента 
            linkedList.Add("drawing"); //добавление пятого элемента 
            linkedList.Add("history"); //добавление шестого элемента 
            linkedList.Add("chemistry"); //добавление седьмого элемента 

            foreach (var spisok in linkedList) //вывод всех элементов нашего списка на консоль
            {
                Console.WriteLine(spisok);
            }


            linkedList.Delete("drawing"); //удаление элемента из нашего списка

            linkedList.AppendFirst("works"); //добавление элемента в начало нашего списка 

            bool item_name = linkedList.Contains("works"); //проверяем отсутствие или присутстввие элемента в нашем списке 
            if (item_name)//условная конструкция для проверки наличия
            {
                Console.WriteLine("\nThe works is in the schedule");
            }
            else
            {
                Console.WriteLine("\nThere is no works in the schedule");
            }
            linkedList.Clear();//очистка нашего списка
            linkedList.Add("astronomy"); //добавление нового элемента 
            foreach (var spisok in linkedList) //вывод всех элементов нашего списка на консоль
            {
                Console.WriteLine(spisok);
            }*/
            

               
       
            
        }
    }
}
